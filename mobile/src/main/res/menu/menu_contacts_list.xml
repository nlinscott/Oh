<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".ContactsListActivity">

    <item android:id="@+id/add"
        app:showAsAction="always"
        android:showAsAction="always"
        android:icon="@android:drawable/ic_menu_add"/>

    <item android:id="@+id/remove"
        android:orderInCategory="98"
        android:title="@string/title_activity_remove_favorites"
        app:showAsAction="never"
        android:showAsAction="never"/>

    <item android:id="@+id/sync"
        android:orderInCategory="99"
        android:title="@string/sync_now"
        app:showAsAction="never"
        android:showAsAction="never"/>

    <item android:id="@+id/action_settings" android:title="@string/action_settings"
        android:orderInCategory="100" app:showAsAction="never" />
</menu>
